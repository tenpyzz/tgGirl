# 🔧 Настройка переменных окружения

## 📝 Для локальной разработки

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
# Telegram Bot Token
TELEGRAM_BOT_TOKEN=8482617305:AAHnQV93IUQ0dkz67fPaG-hTQCmMj-bkXpw

# Railway App URL
NEXT_PUBLIC_APP_URL=https://web-production-877f.up.railway.app

# Webhook URL для production
WEBHOOK_URL=https://web-production-877f.up.railway.app/api/webhook

# OpenRouter API Key (добавьте свой ключ)
# Получите на https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Database URL (для локальной разработки используйте PostgreSQL)
DATABASE_URL=your_local_database_url_here
```

## 🚀 Для Railway (Production)

В настройках вашего сервиса на Railway перейдите в раздел **"Variables"** и добавьте следующие переменные:

### Обязательные переменные:

```
TELEGRAM_BOT_TOKEN=8482617305:AAHnQV93IUQ0dkz67fPaG-hTQCmMj-bkXpw
NEXT_PUBLIC_APP_URL=https://web-production-877f.up.railway.app
WEBHOOK_URL=https://web-production-877f.up.railway.app/api/webhook
```

### Дополнительные переменные:

```
OPENROUTER_API_KEY=your_openrouter_api_key_here
NODE_ENV=production
```

**Важно:** 
- `DATABASE_URL` будет автоматически добавлен Railway, если вы подключили PostgreSQL базу данных
- `WEBHOOK_URL` должен указывать на ваш Railway URL + `/api/webhook`

## 🤖 Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newapp`
3. Выберите вашего бота
4. Укажите название: `ИИ Чат` (или любое другое)
5. Укажите описание: `Общение с ИИ-девушками`
6. Укажите URL: `https://web-production-877f.up.railway.app`
7. Загрузите иконку (опционально)

## ✅ Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите на кнопку "Открыть Mini App 👉"
4. Вы должны увидеть главную страницу приложения

## 🔍 Troubleshooting

### Проблема: Бот не отвечает

**Решение:**
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
- Убедитесь, что webhook установлен (для production)
- Проверьте логи в Railway

### Проблема: Mini App не открывается

**Решение:**
- Убедитесь, что URL в BotFather правильный (HTTPS)
- Проверьте, что приложение доступно по этому URL
- Убедитесь, что `NEXT_PUBLIC_APP_URL` установлен в Railway

### Проблема: Webhook не работает

**Решение:**
- Убедитесь, что `WEBHOOK_URL` правильно указан
- Проверьте, что endpoint `/api/webhook` доступен
- Перезапустите сервис в Railway после добавления переменных

