# 🔄 Инструкция по применению изменений

## Что изменилось

1. **Мини-приложение теперь только для выбора девочки** - после выбора приложение закрывается и пользователь переходит в чат с ботом
2. **Бот общается от лица выбранной девочки** - все сообщения обрабатываются ботом через OpenRouter API
3. **Добавлено поле `selectedGirlId` в модель User** - хранит выбранную девочку для каждого пользователя

## Шаги для применения изменений

### 1. Обновите базу данных

Примените изменения схемы Prisma к вашей базе данных:

```bash
# Сгенерируйте Prisma Client с новой схемой
npm run db:generate

# Примените изменения к базе данных
npm run db:push
```

**Важно:** Если у вас уже есть данные в базе, поле `selectedGirlId` будет `null` для всех существующих пользователей. Это нормально - пользователи смогут выбрать девочку при следующем использовании мини-приложения.

### 2. Перезапустите приложение

После применения изменений в базе данных перезапустите приложение:

```bash
# Для локальной разработки
npm run dev

# Для production на Railway
# Приложение перезапустится автоматически после деплоя
```

### 3. Проверьте работу

1. Откройте мини-приложение в Telegram
2. Выберите девочку
3. Мини-приложение должно закрыться и открыть чат с ботом
4. Напишите боту сообщение - он должен ответить от лица выбранной девочки

## Что делать, если что-то не работает

### Проблема: Ошибка EPERM при генерации Prisma Client (Windows)

Если при выполнении `npm run db:generate` возникает ошибка `EPERM: operation not permitted`, это означает, что файл заблокирован другим процессом. Решение:

**Вариант 1: Закрыть все процессы Node.js (рекомендуется)**

```powershell
# В PowerShell выполните:
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force

# Затем удалите папку .prisma и перегенерируйте:
Remove-Item -Recurse -Force node_modules\.prisma
npm run db:generate
```

**Вариант 2: Перезагрузить компьютер**

Если первый вариант не помог, перезагрузите компьютер и попробуйте снова.

**Вариант 3: Временно отключить OneDrive синхронизацию**

Если проект находится в папке OneDrive, временно отключите синхронизацию для этой папки или переместите проект в другую папку.

### Проблема: Ошибка при применении миграции

Если при выполнении `npm run db:push` возникает ошибка, попробуйте:

```bash
# Сначала сгенерируйте Prisma Client
npm run db:generate

# Затем примените изменения с флагом --accept-data-loss (если нужно)
npx prisma db push --accept-data-loss
```

### Проблема: Бот не отвечает

1. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен в переменных окружения
2. Проверьте, что `OPENROUTER_API_KEY` установлен и валиден
3. Проверьте логи приложения на наличие ошибок

### Проблема: Мини-приложение не закрывается

1. Убедитесь, что вы используете последнюю версию Telegram
2. Проверьте, что `NEXT_PUBLIC_APP_URL` правильно установлен
3. Проверьте консоль браузера на наличие ошибок JavaScript

## Дополнительные настройки

### Переменные окружения

Убедитесь, что у вас установлены следующие переменные окружения:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
OPENROUTER_API_KEY=ваш_ключ_openrouter
DATABASE_URL=ваш_url_базы_данных
NEXT_PUBLIC_APP_URL=ваш_url_приложения
WEBHOOK_URL=ваш_url_приложения/api/webhook
```

## Вопросы и поддержка

Если у вас возникли проблемы, проверьте:
- Логи приложения на Railway
- Логи бота в консоли
- Схему базы данных через Prisma Studio: `npm run db:studio`

