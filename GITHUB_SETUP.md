# 🚀 Простая инструкция: Как подключить GitHub к Railway

## Шаг 1: Создай репозиторий на GitHub

1. Открой https://github.com
2. Нажми зеленую кнопку **"New"** (или "Создать репозиторий")
3. Дай название репозиторию (например: `tgMiniAPP`)
4. **ВАЖНО:** НЕ ставь галочки на:
   - ❌ Add a README file
   - ❌ Add .gitignore
   - ❌ Choose a license
5. Нажми **"Create repository"**

## Шаг 2: Скопируй URL репозитория

После создания GitHub покажет тебе страницу с инструкциями. 
**Скопируй URL** - он будет выглядеть примерно так:
```
https://github.com/ТВОЙ_НИК/tgMiniAPP.git
```

## Шаг 3: Выполни команды в терминале

Открой терминал в этой папке (`tgMiniAPP-clean`) и выполни:

```bash
git remote add origin СЮДА_ВСТАВЬ_СВОЙ_URL
git push -u origin main
```

**Пример:**
```bash
git remote add origin https://github.com/username/tgMiniAPP.git
git push -u origin main
```

Когда выполнишь эти команды, GitHub попросит логин/пароль (или токен).

## Шаг 4: В Railway

1. Открой свой проект в Railway
2. Перейди в **Settings** (Настройки)
3. Найди раздел **"GitHub"** или **"Connect Repository"**
4. Выбери свой репозиторий
5. Выбери ветку **`main`**
6. Готово! 🎉

---

## 💡 Если что-то не работает:

1. **Не можешь залогиниться в GitHub?**
   - Используй Personal Access Token вместо пароля
   - Создай токен здесь: https://github.com/settings/tokens
   - Выбери права: `repo` (полный доступ к репозиториям)

2. **Ошибка "remote already exists"?**
   - Выполни: `git remote remove origin`
   - Потом снова: `git remote add origin ТВОЙ_URL`

3. **Ошибка при push?**
   - Убедись, что репозиторий создан правильно (без README)
   - Проверь, что ты скопировал правильный URL

