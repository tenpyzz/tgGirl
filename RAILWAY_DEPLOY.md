# 🚀 Деплой на Railway через GitHub

Пошаговая инструкция по деплою проекта на Railway через GitHub интеграцию.

## 📋 Подготовка

### 1. Убедитесь, что проект загружен в GitHub

```bash
# Если еще не инициализирован git
git init
git add .
git commit -m "Initial commit"

# Добавьте remote репозиторий
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

## 🎯 Шаги деплоя на Railway

### Шаг 1: Создание проекта на Railway

1. Перейдите на [Railway.app](https://railway.app/)
2. Войдите через GitHub (если еще не вошли)
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий с проектом

### Шаг 2: Добавление PostgreSQL базы данных

1. В вашем проекте на Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных и переменную окружения `DATABASE_URL`

### Шаг 3: Настройка переменных окружения

В настройках вашего сервиса (не базы данных) перейдите в раздел **"Variables"** и добавьте:

#### Обязательные переменные:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather
OPENROUTER_API_KEY=ваш_openrouter_api_ключ
```

#### Дополнительные переменные (опционально):

```
NEXT_PUBLIC_APP_URL=https://ваш-проект.railway.app
WEBHOOK_URL=https://ваш-проект.railway.app/api/webhook
NODE_ENV=production
```

**Важно:** 
- `DATABASE_URL` уже будет автоматически добавлен Railway из PostgreSQL сервиса
- `WEBHOOK_URL` нужен для работы Telegram webhook в production

### Шаг 4: Настройка деплоя

Railway автоматически определит Next.js проект и настроит деплой. Убедитесь что:

1. **Root Directory** (если проект в подпапке): оставьте пустым или укажите путь
2. **Build Command**: `npm run build` (Railway определит автоматически)
3. **Start Command**: `npm start` (Railway определит автоматически)

### Шаг 5: Первый деплой

1. Railway автоматически начнет деплой после подключения репозитория
2. Дождитесь завершения сборки (может занять 3-5 минут)
3. После успешного деплоя Railway создаст публичный URL для вашего приложения

### Шаг 6: Заполнение базы данных

После первого деплоя нужно заполнить базу данных начальными данными:

1. В Railway перейдите в ваш сервис
2. Откройте вкладку **"Deployments"**
3. Найдите последний успешный деплой
4. Нажмите на **"..."** → **"View Logs"**
5. Или используйте **"Connect"** → **"Shell"** для доступа к терминалу

В терминале выполните:

```bash
npm run seed
```

Это создаст первую девушку "Анна" в базе данных.

### Шаг 7: Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newapp`
3. Выберите вашего бота
4. Укажите название: `ИИ Чат` (или любое другое)
5. Укажите описание: `Общение с ИИ-девушками`
6. Укажите URL: `https://ваш-проект.railway.app` (ваш Railway URL)
7. Загрузите иконку (опционально)

### Шаг 8: Настройка Webhook (если нужно)

Если вы хотите использовать webhook вместо polling:

1. Убедитесь, что в переменных окружения указан `WEBHOOK_URL`
2. Перезапустите сервис в Railway
3. Webhook автоматически установится при запуске (см. `lib/telegram.ts`)

## 🔍 Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите на кнопку "Открыть Mini App"
4. Вы должны увидеть главную страницу с девушкой "Анна"
5. Нажмите на карточку девушки и попробуйте отправить сообщение

## 🛠️ Troubleshooting

### Проблема: Сборка не проходит

**Решение:**
- Проверьте логи деплоя в Railway
- Убедитесь, что все переменные окружения установлены
- Проверьте, что `DATABASE_URL` правильно подключен

### Проблема: Ошибка Prisma

**Решение:**
- Убедитесь, что PostgreSQL база данных создана
- Проверьте, что `DATABASE_URL` корректный
- В логах деплоя должна быть команда `prisma generate` и `prisma db push`

### Проблема: Приложение не запускается

**Решение:**
- Проверьте логи в Railway
- Убедитесь, что все переменные окружения заполнены
- Проверьте, что порт настроен правильно (Railway автоматически устанавливает `PORT`)

### Проблема: Telegram Mini App не открывается

**Решение:**
- Убедитесь, что URL в BotFather правильный (HTTPS)
- Проверьте, что приложение доступно по этому URL
- Убедитесь, что SSL сертификат валидный (Railway делает это автоматически)

### Проблема: База данных пустая

**Решение:**
- Выполните `npm run seed` через Railway Shell
- Или выполните вручную через терминал деплоя

## 📝 Автоматический деплой

Railway автоматически деплоит проект при каждом push в основную ветку (обычно `main` или `master`).

Чтобы отключить автоматический деплой:
1. Перейдите в настройки сервиса
2. В разделе **"Settings"** → **"Source"**
3. Отключите **"Auto Deploy"**

## 🔄 Обновление проекта

После изменений в коде:

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Ваше сообщение"
   git push
   ```

2. Railway автоматически начнет новый деплой
3. Дождитесь завершения сборки
4. Проверьте работу приложения

## 📊 Мониторинг

- **Logs**: Просматривайте логи в реальном времени в Railway
- **Metrics**: Мониторьте использование CPU, памяти и сети
- **Deployments**: История всех деплоев и возможность отката

## 💰 Важные замечания

1. **PostgreSQL**: Railway предоставляет бесплатный тариф с ограничениями
2. **Хостинг**: Railway предоставляет бесплатный тариф с ограничениями трафика
3. **OpenRouter API**: Используется ваша квота, следите за расходами
4. **Telegram Bot**: Бесплатный, но есть лимиты на количество запросов

## 🎉 Готово!

Ваш проект теперь работает на Railway! 🚀

Если возникли проблемы - проверьте логи в Railway и убедитесь, что все переменные окружения установлены правильно.

