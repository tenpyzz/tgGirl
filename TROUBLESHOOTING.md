# 🔧 Решение проблем с деплоем на Railway

## ❌ Приложение не запускается после деплоя

### Проблема: "Application failed to respond"

#### 1. Проверьте логи деплоя

1. В Railway Dashboard откройте ваш сервис
2. Перейдите в раздел **"Deployments"**
3. Найдите последний деплой
4. Откройте **"View Logs"** или **"Deploy Logs"**

#### 2. Частые причины и решения

##### Ошибка: "TELEGRAM_BOT_TOKEN не установлен"

**Решение:**
- Перейдите в настройки сервиса → **"Variables"**
- Добавьте переменную `TELEGRAM_BOT_TOKEN=ваш_токен`
- Перезапустите сервис

##### Ошибка: "DATABASE_URL не установлен"

**Решение:**
- Убедитесь, что PostgreSQL база данных создана в Railway
- Railway автоматически создает `DATABASE_URL`, но проверьте в **"Variables"**
- Если базы данных нет, создайте: **"+ New"** → **"Database"** → **"Add PostgreSQL"**

##### Ошибка: "Can't reach database server"

**Решение:**
- Убедитесь, что база данных запущена
- Проверьте, что `DATABASE_URL` правильный
- Перезапустите базу данных в Railway

##### Ошибка: "Migration failed"

**Решение:**
- Это не критично - приложение запустится даже если миграция не удалась
- Выполните миграцию вручную через Railway Shell:
  ```bash
  npx prisma db push
  ```

##### Ошибка: "Port already in use"

**Решение:**
- Railway автоматически устанавливает переменную `PORT`
- Next.js автоматически использует эту переменную
- Если ошибка сохраняется, проверьте, что нет конфликта портов

#### 3. Проверка переменных окружения

Убедитесь, что в Railway Dashboard → **"Variables"** установлены:

**Обязательные:**
```
TELEGRAM_BOT_TOKEN=8482617305:AAHnQV93IUQ0dkz67fPaG-hTQCmMj-bkXpw
NEXT_PUBLIC_APP_URL=https://web-production-877f.up.railway.app
WEBHOOK_URL=https://web-production-877f.up.railway.app/api/webhook
```

**Дополнительные:**
```
OPENROUTER_API_KEY=ваш_ключ
NODE_ENV=production
```

**Автоматические (Railway создает):**
```
DATABASE_URL=postgresql://...
PORT=3000
```

#### 4. Проверка базы данных

1. Убедитесь, что PostgreSQL база данных создана
2. Выполните миграции через Railway Shell:
   ```bash
   npx prisma db push
   ```
3. Заполните базу данных начальными данными:
   ```bash
   npm run seed
   ```

#### 5. Перезапуск сервиса

После изменения переменных окружения:
1. Перейдите в настройки сервиса
2. Нажмите **"Restart"** или **"Redeploy"**
3. Дождитесь завершения деплоя

## 🔍 Дополнительная диагностика

### Просмотр логов в реальном времени

1. В Railway Dashboard откройте ваш сервис
2. Перейдите в раздел **"Logs"**
3. Вы увидите логи в реальном времени

### Проверка через Railway Shell

1. В Railway Dashboard откройте ваш сервис
2. Нажмите **"Connect"** → **"Shell"**
3. Выполните команды для диагностики:
   ```bash
   # Проверка переменных окружения
   env | grep TELEGRAM
   env | grep DATABASE
   
   # Проверка подключения к базе данных
   npx prisma db push
   
   # Проверка работы приложения
   npm start
   ```

## 📞 Если проблема не решена

1. Проверьте логи деплоя в Railway
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных доступна
4. Попробуйте пересоздать сервис в Railway
5. Обратитесь в Railway Support через Help Station

